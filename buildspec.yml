version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies..."
      - npm ci  # or 'npm install' if you prefer

    build:
      commands:
        - echo "Building Next.js project..."
        - npm run build



artifacts:
  files:
    - '**/*'
  base-directory: out
  discard-paths: yes